# 2026-05-23

## 第二轮样式优化（参考 tb-front）
参考 `D:\Adobe\webstorm\tb-front` 的样式系统，进一步优化了以下方面：

### App.vue
- 将静态伪元素光晕 (`::before/::after` + `bgOrbFloat` keyframes) 替换为 **Canvas 动态背景**
- Canvas 渲染 30 个几何图形（圆形/矩形/三角形）+ 20 个光点粒子
- 颜色方案：`#667eea`, `#a0a0ff`, `#00d9c0`, `#ffb347`, `#ba55d3`
- 添加 `fade` 路由过渡：`<transition name="fade" mode="out-in">` + CSS 类（opacity + translateY）

### ChatView.vue
- btn-play：胶囊形、hover 上移 + 阴影、active 缩小
- btn-send：active 增加 scale(0.97) 按下效果
- chat-container / live2d-sidebar：hover 边框高亮过渡

### SettingsView.vue  
- 全部按钮统一胶囊形 (border-radius: 20px)
- btn-save：改为描边风格，hover 填充 + 上移
- btn-test：shimmer + hover 上移 + active 缩小
- btn-switch：hover 上移 + 阴影
- btn-default：hover 上移 + 阴影 + active 缩小
- btn-apply-params：新增 shimmer 光扫动效 + active
- btn-add：虚线边框 hover 变实线 + 上移
- details 展开内容添加 fadeIn 动画

### Live2DSettings.vue
- 预览控制按钮：胶囊形 + hover 上移 + active 缩小
- btn-reset：胶囊形 + hover 上移 + 阴影 + active 缩小
- 开关 toggle：cubic-bezier 缓动过渡
- settings-section：hover 增加 box-shadow
- 危险按钮：hover 颜色增强 + active 缩小

开发服务器运行正常：`http://localhost:5174` (HTTP 200)
